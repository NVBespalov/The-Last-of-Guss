export const ROUTES = {
    LOGIN: '/login',
    ROUNDS: '/rounds',
    ROUND: '/round',
} as const

export const ROUND_STATUS = {
    COOLDOWN: 'cooldown',
    ACTIVE: 'active',
    FINISHED: 'finished',
} as const
